apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "revive-charts.fullname" . }}-test-connection"
  labels:
    {{- include "revive-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "revive-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
