apiVersion: v1
kind: Service
metadata:
  name: {{ include "revive-charts.fullname" . }}
  labels:
    {{- include "revive-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "revive-charts.selectorLabels" . | nindent 4 }}
